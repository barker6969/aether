# Tauri build outputs
target/
gen/

# Bundle staging
dist/
build/

# Generated icons — committed to the repo so CI doesn't have to regenerate.
# (Regenerate from src-tauri/icons/source.png via `yarn tauri icon` and commit
#  the result. Keeping them in git makes builds deterministic.)

# Node
node_modules/
.yarn/
yarn-error.log
package-lock.json

# OS
.DS_Store
Thumbs.db
